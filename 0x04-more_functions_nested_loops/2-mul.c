#include "main.h"
/**
 * mul(int a, int b)- function to multiply 2 numbers
 * @a: first number
 * @b: second number
 * Return: returns the numbers multiplication
*/

int mul(int a, int b)
{
	return (a * b);
}
