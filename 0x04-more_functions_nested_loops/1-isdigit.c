#include "main.h"

/**
 * _isdigit - checks if input is digit 0-9
 * 
 *@c : input to be checked
 *Return: 0 if digit , 1 otherwise
*/


int _isdigit(int c)
{
	if (c >= 0 && c =< 9)
	{
		return (0)
	}
	else 
	{
		return (1)
	}
}
