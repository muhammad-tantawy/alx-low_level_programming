#include <stdlib.h>
#include <stdio.h>
#include "dog.h"
dog_t *new_dog(char *name, float age, char *owner)
{
	char *name2 = name;
	char *owner2 = owner;

	if (new_dog == NULL)
	{
		return (NULL);
	}
}
