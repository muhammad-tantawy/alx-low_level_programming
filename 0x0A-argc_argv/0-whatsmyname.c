#include "main.h"
#include <stdio.h>

/**
 * main - function prints the file name
 * @argc: the number of arguments
 * @argv: the value of the argemnts
 * Return: 0
 */

int main(int __attribute__((unused)) argc, char *argv[])

{
	printf("%s\n", argv[0]);
	return (0);
}
